<h1>Mes dates</h1>
<div class="content">
    <div >
      <% @bookings.each do |booking|%>
        <% booking.stage.photos.each do |photo| %>
          <%= cl_image_tag(photo.key, class: "photos")  %>
        <% end %>
        <%= booking.stage.name %>
        <%= booking.date_start %>
        <%= booking.status %>
        <hr>
      <% end %>
    </div>
</div>
