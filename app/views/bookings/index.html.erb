<%= render("shared/main_page_header", title:"Mes réservations") %>
<div class="bookings">
    <div class="booking-container">
      <% @bookings.each do |booking|%>
        <%= render("shared/booking_card", booking: booking, fill: true) %>
      <% end %>
    </div>
</div>
